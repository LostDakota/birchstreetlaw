Options -MultiViews
RewriteEngine On
RewriteBase /

# Serve existing files/directories directly
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Rewrite /page and /page/ to /page.html
RewriteRule ^([^/]+)/?$ /$1.html [L]
